#pragma once

#define _USE_MATH_DEFINES
#include <cmath>

const double PI = atan(1.0)*4.0;
const double R2D = 180.0 / PI;  //rads to degrees factor

const int PRE = 1;
const int POST = 2;
const int EXT = 3;

const int MAX_NUM_FILES = 100;
const int MAX_LINE_SIZE = 32768;
const int MAX_SOL_NAME_SIZE = 40;
